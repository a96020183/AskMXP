# ─────────────────────────────────────────────────────────────
# AskMXP Secrets — Streamlit Cloud deployment template
#
# DO NOT commit the real secrets.toml file. On Streamlit Cloud,
# paste these key/value pairs into the Secrets panel (not this file).
# Locally you can either use .env (preferred) or copy this file to
# .streamlit/secrets.toml and fill in the values.
# ─────────────────────────────────────────────────────────────

MIXPANEL_USERNAME = ""
MIXPANEL_SECRET = ""
MIXPANEL_PROJECT_ID = ""
ANTHROPIC_API_KEY = ""
GOOGLE_CHAT_WEBHOOK = ""
